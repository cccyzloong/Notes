## 命名空间与Autoload
- 类的自动载入
   + __autoload(已废弃)
   ```
    function __autoload($class){
        require __DIR__.'/'.$class.'.php';
    }
    ```
   + spl_autoload_register()可以注册多个
   ```
   spl_autoload_register('autoload1');
   function autoload1($class){
        require __DIR__.'/'.$class.'.php';
   }
   ```
 - 开发一个PSR-0的基础框架
## SPL库的使用(PHP标准库)
- php 链式操作的实现
- php 魔术方法
1. get/set
2. call/callStatic 
3. toString
4. invoke 把对象当成函数使用时触发。
## php设计模式
- 工厂模式  工厂方法或者类生成对象，而不是在代码中直接new
- 单例模式  使某个类的对象仅允许创建一个
- 注册模式  全局共享和交换对象
- 适配器模式 可以将截然不同的函数封装成统一的API
- 策略模式 将一组特定的行为和算法封装成类，以适应某些特定的上下文环境，这种模式就是策略模式
- 数据对象映射模式 
   + 数据对象映射模式，是将对象和数据存储映射起来，对一个对象的操作会映射为对数据存储的操作
   + 在代码中实现数据对象映射模式，我们将实现一个ORM类，经复杂的SQL语句映射成对象属性的操作
   + 结合使用数据对象映射模式，工厂模式，注册模式
- 观察者模式
   + 观察者模式(Oberver),当一个对象状态发生改变时，依赖它的对象全部会收到通知，并自动更新。
   + 场景：一个事件发生后，要执行一连串更新操作。传统的编程方式，就是在事件的代码之后直接加入处理逻辑。当更新的逻辑增多之后，代码会变得难以维护。这种方式是耦合的，浸入式的，增加新的逻辑需要修改事件主体的代码
   + 观察者模式实现了低耦合，非侵入式的通知与更新机制。